server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/finance_tracker?creatIfNotExist=tSSL=false&allowPubKeyRetrieval=true&serverTimezone=UTC
    username: ${MYSQL_USER:root}
    password: ${MYSQL_PASSWORD:root123}
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true

telegram:
  bot:
    token: ${TELEGRAM_BOT_TOKEN:8846285563:AAEy dsr20sb27ACzQrE}//Dummy token  insert your token here 
    username: ${TELEGRAM_BOT_USERNAME:_bot name }

logging:
  level:
    com.financetracker.bot: INFO
