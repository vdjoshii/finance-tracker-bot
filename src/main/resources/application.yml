server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/finance_tracker?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: ${MYSQL_USER:root}
    password: ${MYSQL_PASSWORD:root123}
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true

telegram:
  bot:
    token: ${TELEGRAM_BOT_TOKEN:8846285563:AAEyyjskrglwxtqXX4Q2dsr20sb27ACzQrE}
    username: ${TELEGRAM_BOT_USERNAME:Viraj_Kharcha_Khata_bot}

logging:
  level:
    com.financetracker.bot: INFO